int fact(int n) {
  if (n <= 1) {
    return 1;
  } else {
    return n * fact(n - 1);
  }
}

int fib(int n) {
  if (n < 2) {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

int main() {
  int x = 10;
  {
    int x = 4;
    x = x + fact(3);
  }
  return fact(4) + fib(6) + x;
}
