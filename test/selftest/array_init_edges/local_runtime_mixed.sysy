int main() {
  int x = 5;
  int a[2][3] = {{x}, x + 1, {x + 2, x + 3}};
  int result = a[0][0] * 10000
      + a[0][1] * 1000
      + a[0][2] * 100
      + a[1][0] * 10
      + a[1][1]
      + a[1][2];
  putint(result);
  return 0;
}
