const int n = 2;
int g[2][3] = {{n}, n + 1, {n + 2, n + 3}};

int main() {
  int result = g[0][0] * 10000
      + g[0][1] * 1000
      + g[0][2] * 100
      + g[1][0] * 10
      + g[1][1]
      + g[1][2];
  putint(result);
  return 0;
}
