[package]
name = "compile-sysy"
version = "0.1.0"
edition = "2021"

[build-dependencies]
lalrpop = "0.22"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
lalrpop-util = { version = "0.22", features = ["lexer", "unicode"] }
